import { Meta } from "@storybook/addon-docs/blocks";
import { SpacingBar } from "./SpacingBar";

<Meta title="Foundations/Spacing" />

export const scale = [
  { step: 0, px: 0 },
  { step: 1, px: 4 },
  { step: 2, px: 8 },
  { step: 3, px: 12 },
  { step: 4, px: 16 },
  { step: 6, px: 24 },
  { step: 8, px: 32 },
  { step: 12, px: 48 },
  { step: 16, px: 64 },
  { step: 24, px: 96 },
];

<div className="font-sans">

# Spacing

`tokens.css` defines no custom spacing tokens, so spacing uses **Tailwind's default scale**
(base unit `4px`). The same step applies across `p-*`, `m-*`, `gap-*`, `w-*`, `h-*`, etc. —
e.g. step `4` is `p-4` / `gap-4` / `mt-4` = `16px`.

<div className="flex flex-col gap-1 py-2">
  {scale.map((s) => (
    <SpacingBar key={s.step} {...s} />
  ))}
</div>

</div>
